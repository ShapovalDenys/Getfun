import React from 'react';

import './Slogan.scss';

const Slogan = () => (
  <div className="slogan">
    <h3>We always here, when you need us</h3>
    <span>Use our services on any device</span>
    <a className="slogan__link" href="#header">Get Started</a>
  </div>
);

export default Slogan;
